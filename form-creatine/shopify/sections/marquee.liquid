<div class="marquee">
  <div class="marquee__track">
    {%- for i in (1..2) -%}
      <span>
        {%- for block in section.blocks -%}{{ block.settings.text }}{%- unless forloop.last %}</span><span>{% endunless -%}{%- endfor -%}
      </span>
    {%- endfor -%}
  </div>
</div>

{% schema %}
{
  "name": "Marquee",
  "blocks": [
    { "type": "item", "name": "Logo / text", "settings": [ { "type": "text", "id": "text", "label": "Text", "default": "As seen in WOMEN'S HEALTH" } ] }
  ],
  "presets": [
    {
      "name": "Marquee",
      "blocks": [
        { "type": "item", "settings": { "text": "WOMEN'S HEALTH" } },
        { "type": "item", "settings": { "text": "MEN'S HEALTH" } },
        { "type": "item", "settings": { "text": "GQ" } },
        { "type": "item", "settings": { "text": "WELL+GOOD" } },
        { "type": "item", "settings": { "text": "SELF" } }
      ]
    }
  ]
}
{% endschema %}
