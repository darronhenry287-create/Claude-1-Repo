<section class="seals">
  <div class="wrap seals__row">
    {%- for block in section.blocks -%}
      <div class="badge2" {{ block.shopify_attributes }}>
        <span class="ring">{% render 'icon', name: 'check' %}</span>
        <span>{{ block.settings.text }}</span>
      </div>
    {%- endfor -%}
  </div>
</section>

{% schema %}
{
  "name": "Seal row",
  "blocks": [ { "type": "seal", "name": "Seal", "settings": [ { "type": "text", "id": "text", "label": "Label", "default": "Vegan" } ] } ],
  "max_blocks": 8,
  "presets": [
    { "name": "Seal row", "blocks": [
      { "type": "seal", "settings": { "text": "Vegan" } },
      { "type": "seal", "settings": { "text": "Cruelty-free" } },
      { "type": "seal", "settings": { "text": "Silicone-free" } },
      { "type": "seal", "settings": { "text": "Dermatologist-tested" } },
      { "type": "seal", "settings": { "text": "Non-comedogenic" } },
      { "type": "seal", "settings": { "text": "90-day promise" } }
    ] }
  ]
}
{% endschema %}
