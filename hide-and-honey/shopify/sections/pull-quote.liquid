<section class="section">
  <div class="wrap pullquote">
    <div class="rte" style="font-family:var(--slab);font-weight:800;font-size:clamp(1.6rem,4vw,2.6rem);line-height:1.2">{{ section.settings.quote }}</div>
    {%- if section.settings.by != blank -%}<div class="by">{{ section.settings.by }}</div>{%- endif -%}
  </div>
</section>
{% schema %}
{
  "name": "Pull quote",
  "settings": [
    { "type": "richtext", "id": "quote", "label": "Quote", "default": "<p>\"We didn't set out to start a skincare company. We just couldn't go back to complicated once we'd tried simple.\"</p>" },
    { "type": "text", "id": "by", "label": "Attribution", "default": "— Jess, founder" }
  ],
  "presets": [ { "name": "Pull quote" } ]
}
{% endschema %}
